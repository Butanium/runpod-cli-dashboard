defaults:
  - config
  - _self_



remote_command: |
  bash -c 'git clone https://github.com/science-of-finetuning/diffing-toolkit/
  cd diffing-toolkit
  git checkout weight-ampl-debug
  uv sync --extra vllm
  uv run streamlit run dashboard.py'
